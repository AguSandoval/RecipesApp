import React from "react";
import Card from "./Card";
import SearchBar from "./SearchBar";

const RecipeApp = () => {
    return (
        <div>
            <SearchBar />
            <Card />
        </div>
    );
};

export default RecipeApp;
